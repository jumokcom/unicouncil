/**
 * 역할: 유틸리티 함수 인덱스 - 범용적으로 사용되는 헬퍼 함수들의 진입점
 * 연결: 모든 컴포넌트, 훅에서 import하여 공통 로직 재사용
 * 의존성: clsx, tailwind-merge (설치 필요)
 * 포함: cn 함수 (Tailwind 클래스 조건부 병합)
 */

// import { type ClassValue, clsx } from "clsx"
// import { twMerge } from "tailwind-merge"

// export function cn(...inputs: ClassValue[]) {
//   return twMerge(clsx(inputs))
// }

// 패키지 설치 후 주석 해제: npm install clsx tailwind-merge
