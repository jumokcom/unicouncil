/**
 * 역할: Next.js 설정 파일 - 빌드 및 런타임 옵션 설정
 * 연결: 전체 Next.js 앱 빌드 프로세스에 영향
 * 의존성: Next.js 프레임워크
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
