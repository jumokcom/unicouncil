/**
 * 역할: 기본 버튼 컴포넌트 - 재사용 가능한 UI 버튼
 * 연결: 모든 페이지에서 import하여 사용
 * 의존성: Tailwind CSS, utils/index.ts의 cn 함수
 * 스타일: variants (primary, secondary, outline 등) 지원
 */

// shadcn/ui Button 컴포넌트 구현 예정
export function Button() {
  return <button>Button Component</button>
}
