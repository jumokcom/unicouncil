/**
 * 역할: 모달 컴포넌트 - 팝업 창을 위한 UI
 * 연결: 계정 공유, 사진 업로드, 확인 다이얼로그 등에서 사용
 * 의존성: Radix UI (접근성), Tailwind CSS, Portal API
 * 기능: backdrop click close, ESC key close, focus trap
 */

// shadcn/ui Modal/Dialog 컴포넌트 구현 예정
export function Modal() {
  return <div>Modal Component</div>
}

export function ModalContent() {
  return <div>Modal Content</div>
}

export function ModalHeader() {
  return <div>Modal Header</div>
}

export function ModalFooter() {
  return <div>Modal Footer</div>
}
