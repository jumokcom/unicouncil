/**
 * 역할: 타입 정의 인덱스 - 프로젝트 전체에서 사용하는 TypeScript 타입들의 진입점
 * 연결: 모든 컴포넌트, 훅, 유틸리티에서 import
 * 의존성: lib/database.types.ts (Supabase 자동 생성 타입)
 * 포함: User, Post, Comment 등 비즈니스 도메인 타입들
 */

// 커스텀 타입들을 이곳에서 export
// export type { User } from './user';
// export type { Post } from './post';
// export type { Comment } from './comment';

// Supabase 타입 재export
// export type { Database } from '../lib/database.types';
