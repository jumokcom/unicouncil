/**
 * 역할: 신규 부원 신청 페이지 - 동아리 가입 신청서 작성
 * 연결: app/applications/page.tsx (신청 목록), schemas/application.ts (검증)
 * 의존성: React Hook Form, Zod, lib/supabase.ts, components/ui
 * Supabase 기능: Database (applications 테이블 삽입)
 * 폼 검증: 이름, 학과, 학번, 전화번호, 지원동기 등
 */

export default function NewApplicationPage() {
  // 신청서 폼 구현 예정
  return <div>New Application Page</div>
}
