/**
 * 역할: 활동 앨범 목록 페이지 - MT, 프로젝트, 총회 등 앨범 목록 표시
 * 연결: app/albums/[id]/page.tsx (개별 앨범), app/albums/upload/page.tsx (업로드)
 * 의존성: lib/supabase.ts, types/database.types.ts, components/ui
 * Supabase 기능: Database (albums 테이블 조회), Storage (이미지 URL)
 */

export default function AlbumsPage() {
  // 앨범 목록 구현 예정
  return <div>Albums Page</div>
}
