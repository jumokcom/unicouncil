/**
 * 역할: 사진 업로드 페이지 - 앨범에 새로운 사진 업로드
 * 연결: app/albums/page.tsx, app/albums/[id]/page.tsx와 연동
 * 의존성: lib/supabase.ts, components/ui, schemas/
 * Supabase 기능: Storage (파일 업로드), Database (album_photos 삽입)
 * 파일 처리: 이미지 압축, 타입 검증, 크기 제한
 */

export default function UploadPage() {
  // 사진 업로드 구현 예정
  return <div>Upload Page</div>
}
