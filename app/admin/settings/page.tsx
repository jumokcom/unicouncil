/**
 * 역할: 시스템 설정 페이지 - 동아리 기본 정보, 시스템 설정 관리
 * 연결: app/admin/page.tsx에서 라우팅, components/ui/modal.tsx 사용
 * 의존성: lib/supabase.ts, React Hook Form, Zod
 * Supabase 기능: Database (설정 테이블), RLS (관리자만 접근)
 */

export default function SettingsPage() {
  // 시스템 설정 구현 예정
  return <div>Settings Page</div>
}
