/**
 * 역할: 사용자 관리 페이지 - 전체 사용자 권한 및 정보 관리
 * 연결: app/admin/page.tsx에서 라우팅, components/ui 컴포넌트들
 * 의존성: lib/supabase.ts, types/database.types.ts, constants/roles.ts
 * Supabase 기능: Database (user_roles, members 테이블 수정), RLS (관리자만 접근)
 */

export default function UsersPage() {
  // 사용자 관리 구현 예정
  return <div>Users Management Page</div>
}
