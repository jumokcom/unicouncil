/**
 * 역할: 관리자 대시보드 - 시스템 전체 관리 (관리자만)
 * 연결: app/admin/users/page.tsx, app/admin/settings/page.tsx
 * 의존성: lib/supabase.ts, constants/roles.ts, components/layout/auth-guard.tsx
 * Supabase 기능: Database (전체 테이블 접근), RLS (관리자만 접근), Auth (권한 체크)
 */

export default function AdminPage() {
  // 관리자 대시보드 구현 예정
  return <div>Admin Dashboard Page</div>
}
